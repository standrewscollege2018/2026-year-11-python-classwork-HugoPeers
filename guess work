'''Guessing a number between 1 and 10:'''

import random

# Pick a random integer from 1 to 10 
NUMBER = random.randint (1,10)

ask_guess = True 


while ask_guess == True:
    # get the guess and check if it is correct 
    # If correc, set ask_guess to false
    guess = int(input("Guess a number from 1 and 10"))
    if guess == NUMBER: 
        ask_guess = False 
    elif guess < NUMBER:
        print ("Too low")
    else:
        guess > NUMBER 
        print("Too high")
        

print("Well done")
    
    
    


